import { AdminShell } from "@/components/admin/AdminShell";
import { updateBookingStatusAction } from "@/app/admin/actions";
import { prisma } from "@/lib/prisma";

export const dynamic = "force-dynamic";

export default async function BookingsAdminPage() { let bookings: any[] = []; let ready = Boolean(process.env.DATABASE_URL); if (ready) try { bookings = await prisma.booking.findMany({ orderBy: { createdAt: "desc" }, take: 200 }); } catch { ready = false; } return <AdminShell title="Yêu cầu đặt bàn" description="Xác nhận và theo dõi booking gửi từ website.">{!ready && <div className="admin-alert warning">Cần kết nối MySQL để xem booking.</div>}<section className="admin-panel"><div className="admin-panel-heading"><div><h2>Danh sách booking</h2><p>{bookings.length} yêu cầu gần nhất.</p></div></div>{bookings.length ? <div className="admin-table-wrap"><table><thead><tr><th>Khách hàng</th><th>Liên hệ</th><th>Thời gian đến</th><th>Khách</th><th>Ghi chú</th><th>Trạng thái</th></tr></thead><tbody>{bookings.map((booking) => <tr key={booking.id}><td><strong>{booking.name}</strong><small>{booking.locale.toUpperCase()}</small></td><td><a href={`tel:${booking.phone}`}>{booking.phone}</a></td><td>{new Intl.DateTimeFormat("vi-VN", { dateStyle: "medium", timeStyle: "short" }).format(booking.date)}</td><td>{booking.guests}</td><td>{booking.note || "—"}</td><td><form action={updateBookingStatusAction} className="status-form"><input type="hidden" name="id" value={booking.id} /><select name="status" defaultValue={booking.status}><option value="NEW">Mới</option><option value="CONFIRMED">Đã xác nhận</option><option value="COMPLETED">Hoàn tất</option><option value="CANCELLED">Đã huỷ</option></select><button>Lưu</button></form></td></tr>)}</tbody></table></div> : <div className="admin-empty">Chưa có yêu cầu đặt bàn.</div>}</section></AdminShell>; }
